SkillStepType={
    --- 仅等待
    Wait = 0;
    --- 改变坐标(参数1:相对坐标字符串"x,y,z")
    Move = 1;
    --- 播动画(参数1:动画名)
    Anim = 2;
    --- 播放声音(参数1:音频路径)
    Sound = 3;
    --- 结算消耗(参数1:消耗的属性Key字符串,参数2:固定消耗值,参数3:消耗的计算公式)
    Cost = 4;
    --- 生成召唤物(参数1:召唤物Id)
    GenerateObject = 5;
    --- 生成碰撞器(参数1:触发器Id,参数2:伤害计算公式Id,参数3:最终伤害百分比,参数4:"buff1,buff1持续时间单位毫秒,buff1离开范围是否移除buff0或1;buff2,buff2持续时间单位毫秒,buff2离开范围是否移除buff0或1")
    --- 注意：参数4是字符串，逗号和分号分割
    GenerateCollider = 6;
}